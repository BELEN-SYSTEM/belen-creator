#pragma once

/** Colocación en el tablero superior (filas `usada` con time NULL). */
struct UsadaCanvasPlacement {
    int usadaId = 0;
    int piezaId = 0;
    double xCm = 0;
    double yCm = 0;
};
